<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Transaction Update - {{transactionStatus}}</title>
    <style>
      body { 
        font-family: Arial, sans-serif; 
        color: #333; 
        background-color: #f8f9fa; 
        padding: 20px; 
      }
      .container { 
        background: white; 
        border-radius: 8px; 
        padding: 30px; 
        max-width: 600px; 
        margin: auto; 
        box-shadow: 0 0 10px rgba(0,0,0,0.1); 
      }
      .accepted-status {
        color: #28a745;
      }
      .rejected-status {
        color: #dc3545;
      }
      .transaction-details {
        background-color: #f8f9fa;
        padding: 15px;
        border-radius: 5px;
        margin: 20px 0;
      }
      .footer { 
        margin-top: 20px; 
        font-size: 12px; 
        color: #888; 
        border-top: 1px solid #eee;
        padding-top: 15px;
      }
      .action-button {
        display: inline-block; 
        padding: 12px 20px;
        background-color: #007bff; 
        color: #fff !important; 
        text-decoration: none; 
        border-radius: 4px; 
        font-weight: bold;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2 class="{{#if isAccepted}}accepted-status{{else}}rejected-status{{/if}}">
        Transaction {{transactionStatus}}
      </h2>
      
      <p>Dear {{customerName}},</p>
      
      {{#if isAccepted}}
        <p>Your transaction <strong>#{{transactionId}}</strong> has been successfully processed.</p>
        
        <div class="transaction-details">
          <h3>Booking Details:</h3>
          <ul>
            <li><strong>Event:</strong> {{eventName}}</li>
            <li><strong>Date:</strong> {{eventDate}}</li>
            <li><strong>Seats:</strong> {{seatDetails}}</li>
            <li><strong>Total Amount:</strong> {{totalAmount}}</li>
          </ul>
        </div>
        
        <p>Your tickets have been confirmed. You can view your booking details at any time by visiting your account.</p>
        
        <p style="text-align: center;">
          <a href="{{accountLink}}" target="_blank" class="action-button">View My Tickets</a>
        </p>
      {{else}}
        <p>We regret to inform you that your transaction <strong>#{{transactionId}}</strong> could not be processed.</p>
        
        <div class="transaction-details">
          {{#if usedPoints}}
            <p>✓ <strong>{{usedPoints}} reward points</strong> have been returned to your account.</p>
          {{/if}}
          
          {{#if usedVoucher}}
            <p>✓ Your voucher <strong>"{{usedVoucher}}"</strong> has been restored for future use.</p>
          {{/if}}
          
          {{#if usedCoupon}}
            <p>✓ Your coupon <strong>"{{usedCoupon}}"</strong> has been restored for future use.</p>
          {{/if}}
          
          <p>✓ The selected seats have been released back to inventory.</p>
        </div>
        
        <p>If you believe this was an error or would like to try again, please contact our support team.</p>
        
        <p style="text-align: center;">
          <a href="{{supportLink}}" target="_blank" class="action-button">Contact Support</a>
        </p>
      {{/if}}
      
      <div class="footer">
        <p>Thank you for choosing {{companyName}}!</p>
        <p>Need help? Contact us at <a href="mailto:{{supportEmail}}">{{supportEmail}}</a> or call {{supportPhone}}.</p>
        <p>&copy; {{year}} {{companyName}}. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>